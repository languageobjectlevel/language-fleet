# Architecture

language-fleet uses modular services for:

- agent registry
- workload allocation
- lease lifecycle
- scaling policy evaluation
- connector health checks

Contracts are versioned under `contracts/`.
